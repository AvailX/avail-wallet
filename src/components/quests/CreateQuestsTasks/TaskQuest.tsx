import * as React from "react";
import * as mui from "@mui/material";

const TasksQuests: React.FC = () => {
  return (
    <mui.Box>
      <mui.Typography variant="h4">Tasks</mui.Typography>
      <mui.Typography variant="body1">This is the tasks tab content.</mui.Typography>
    </mui.Box>
  );
};

export default TasksQuests;
